# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Get these values from: https://app.supabase.com/project/[project-id]/settings/api

# Required: Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Required: Supabase anonymous/public key (safe to expose in frontend)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Required for backend operations: Supabase service role key (keep secret!)
# ⚠️ WARNING: This key has admin privileges. Never commit to git or expose to frontend.
# Only needed for: test data scripts, server-side operations
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key


# ============================================
# DEVELOPMENT SETTINGS
# ============================================
# Set to 'true' to enable authentication in local development (for testing auth flows)
# Set to 'false' or leave unset for faster UX development (auth disabled locally)
# ⚠️ In production (NODE_ENV=production), auth is ALWAYS enabled regardless of this setting
ENABLE_AUTH_IN_DEV=false


# ============================================
# NETLIFY DEPLOYMENT NOTES
# ============================================
# When deploying to Netlify, set these environment variables:
# 1. Go to: Site settings → Environment variables
# 2. Add the following:
#    - NEXT_PUBLIC_SUPABASE_URL (same as above)
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY (same as above)
#    - SUPABASE_SERVICE_ROLE_KEY (only if running backend scripts)
# 3. DO NOT set ENABLE_AUTH_IN_DEV in production (auth is always enabled)
#
# Authentication behavior:
# - Development (NODE_ENV=development): Controlled by ENABLE_AUTH_IN_DEV flag
# - Production (NODE_ENV=production): Always enabled, no exceptions
#
# Note: Create a .env.local file with your actual values
# .env.local is gitignored for security
